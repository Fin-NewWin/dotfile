#!/bin/sh

killall swww-daemon
killall waybar
killall gammastep
killall xbindkeys
killall dunst

dunst &
swww init &

waybar &
gammastep &

pamixer --default-source --set-volume 20

xbindkeys -f "$XDG_CONFIG_HOME"/xbindkeys/config

setwall
