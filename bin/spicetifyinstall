#!/usr/bin/env bash

chmod a+wr /opt/spotify
chmod a+wr /opt/spotify/Apps -R

bold=$(tput bold)

normal=$(tput sgr0)
echo "-------------------------------------"
echo "Remember to run ${bold} spicetify apply ${normal}"
echo "-------------------------------------"
