#!/bin/sh

# send_notification() {
#     volume=$(pamixer --get-volume)
# }

case $1 in
    up)
        pamixer -u -i 4
        ;;
    down)
        pamixer -u -d 4
        ;;
    mute)
        pamixer -t
        ;;
    mic)
        pamixer --default-source -t
esac
